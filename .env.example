# ─────────────────────────────────────────────────
# Retrivault — 环境配置模板
# 复制此文件为 .env 后编辑：
#   cp .env.example .env
# ─────────────────────────────────────────────────

# ── 必填 ──────────────────────────────────────

# LLM API Key（DeepSeek / OpenAI / 任意兼容 API）
# 用于 LLM 生成 + Query 改写
LLM_API_KEY=sk-your-key-here

# Obsidian vault 路径（你的笔记目录）
# 不配则使用 docs/samples/ 中的示例文档
OBSIDIAN_VAULT_PATH=~/your-vault-path

# ── 可选 ──────────────────────────────────────

# LLM Provider（deepseek | openai | ollama）
LLM_PROVIDER=deepseek

# Embedding API Key（硅基流动 / OpenAI 等）
# 如果不配，会用 LLM_API_KEY 作为兜底
EMBEDDING_API_KEY=

# Embedding API 地址（默认硅基流动，与 config.yaml 的 embedding.base_url 一致）
# 此项会覆盖 config.yaml 中的 base_url
EMBEDDING_BASE_URL=https://api.siliconflow.cn/v1

# Rerank API Key（默认与 Embedding API Key 共用）
RERANK_API_KEY=

# 配置 profile 套餐（default / siliconflow / ollama-offline）
# 不设则用 default；详情查看 profiles/ 目录
# PROFILE=default
